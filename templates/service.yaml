apiVersion: v1
kind: Service
metadata:
  name: java-cicd-app-svc
spec:
  type: ClusterIP
  selector:
    app: java-cicd-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
